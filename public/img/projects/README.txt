Put your real project photos here, named to match the "images" paths
in server/data/projects.json (e.g. 3rd-eye-three-1.jpg).

Until real images are added, the <img> tags in the grid/lightbox will
show broken image icons — that's expected with placeholder data.
